---
description: Distill this session into its context tree in the grove — route via the engine's three bands, write signal-dense owner-voiced leaves, refresh the canopy, reindex, then the acceptance gate: the user reviews the staged diff and their explicit yes becomes the commit. Nothing enters memory without being seen.
---

Read and follow the protocol at `~/Developer/Grove/protocols/consolidate.md`, end to end. That file is the single canonical source — the routing pass may skip a question, never the acceptance; every leaf obeys the signal check and the owner-voice rule; the canopy refresh is mandatory.

Subject hint from the user (may be empty — beat 1 scopes the consolidation either way): $ARGUMENTS
